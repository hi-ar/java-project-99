server:
  port: 8080

spring:
  # Настраиваем подключение к базе данных
  datasource:
    #    settings for render deploy:
    url: postgresql://taskmng_2410_postgre_user:M2Sbyg7obH3IMZpTZXz6LF4YBq8KCDFO@dpg-csej20ogph6c73eqofk0-a/taskmng_2410_postgre
#    url: jdbc:postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}/${PGDATABASE}
#    username: ${PGUSER}
#    password: ${PGPASSWORD}
#    port: ${PORT}
    driverClassName: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
